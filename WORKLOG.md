# WORKLOG — 작업 기록 (chatlog)

이 앱을 만들면서 **무엇을, 왜, 어떻게** 바꿨는지 시간순으로 남기는 곳.
새 작업을 끝낼 때마다 맨 위에 한 덩어리 추가한다. (최신이 위)

형식:
```
## YYYY-MM-DD  제목
- 한 일:
- 왜:
- 어떻게 확인:
- 다음 할 일:
```

---

## 2026-06-20  결과 자동수집(GitHub Actions) + 현재 결과 스냅샷

- **한 일:**
  - GitHub Pages(정적)에 맞춘 자동수집: **Actions cron → `scripts/fetch-results.mjs` → `data/results.json` 커밋 → 앱이 읽기**.
  - `js/app.js`에 자동/수동 **병합 로직**(`eff()`): 같은 경기에 수동 입력 있으면 우선, 없으면 자동. 순위·표·매치업 모두 병합값 사용. 상단에 "🔄 자동 결과 N경기 반영" 상태줄.
  - `.github/workflows/update-results.yml`(2시간 cron + 수동), `scripts/fetch-results.mjs`(영문→한글 팀명 매핑 포함).
  - **현재 결과 스냅샷**: 웹(CBS Sports)에서 확인한 조별리그 32경기 결과를 `data/results.json`에 시드. 32건 전부 실제 일정 matchId와 대조 검증 통과.
  - `WORKFLOW.md`에 켜는 법(무료 키 발급→Secret `FOOTBALL_API_KEY`→권한→Run workflow) 문서화.
- **왜:** "수동 말고 자동" 요청. Pages엔 서버리스가 없어 Vercel 프록시 대신 Actions 방식 채택(키 안전·CORS 무관).
- **확인:** app.js 문법 OK, 병합 순위 검증(멕시코 6/한국 3, 실제와 일치), 결과 matchId 32건 일정 일치, 숨은 제어문자 0.
- **남은 일(사용자):** football-data.org 무료 키 발급 후 Secret 등록하면 그때부터 자동 갱신. 미등록 시에도 스냅샷+수동으로 정상.
- **다음(작업 예정):** 다이어그램(mermaid) 렌더 버그 전수 수정, host-cities 지도/고도/심화, data-football 전문가 심화.

---

## 2026-06-20  GitHub Pages 배포 + 탐구 확장(행정/국제 분야, 허브 신규 2종)

- **한 일:**
  - **공개 배포:** GitHub `jwj-nick/football-2026`(public) 생성·푸시, Pages 활성화(main/root).
    - 🔗 라이브: **https://jwj-nick.github.io/football-2026/** (전 페이지 HTTP 200 확인).
  - **직업 생태계 5번째 분야 추가:** `jobs.html`에 🏛️ 행정/국제기구 클러스터(직업 5종) + 차트 5분류로 갱신.
  - **신규 커리어 페이지:** `explore/careers/admin.html`(행정가·국제기구·LOC·스포츠법률/컴플라이언스·반도핑/윤리; 거버넌스 본진).
  - **허브 신규 메인 2종:** `explore/host-cities.html`(북미 3국 16개 개최도시·시차 가이드), `explore/data-football.html`(스탯·xG·데이터 분석가·앱 데이터 연결).
  - 허브(`explore/index.html`)·기존 커리어 4페이지 pager에 신규 링크 연결.
- **왜:** "지금 상태로 먼저 웹 공개 후 그 위에서 계속 업데이트" 요청 반영. 행정/국제기구는 거버넌스의 본진이라 교육적으로 필요. 허브 2종은 이번 대회(북미 공동개최)·아빠 데이터 분야와 직접 연결.
- **방법:** 배포 먼저 → 핵심 편집(jobs/hub)은 직접, 신규 콘텐츠 3페이지는 병렬 서브에이전트(동일 규약 강제).
- **어떻게 확인:** 내부 링크 무결성(13개 HTML, 깨진 링크 0), div 균형, 라이브 URL 200.
- **다음 할 일:**
  1. 푸시 후 라이브 갱신 확인(1~2분).
  2. 아들 피드백 → 관심 직업/도시 심화, 시차표 정확도 보강(멕시코 DST 주의).
  3. (선택) 실시간 결과 API(미션5), .ics 캘린더.

---

## 2026-06-20  탐구(Explore) 섹션 신설 + 기존 페이지 보강

- **한 일:**
  - **공통 디자인 시스템** 신설: `assets/site.css`(상단 네비바·용어풀이 말풍선·공통 콜아웃), `assets/explore.css`(히어로·카드·타임라인 스텝퍼·직업 블록·생태계 지도·차트 박스).
  - **메인 앱 보강**: 전 페이지 공통 상단 네비게이션, 🇰🇷 한국 첫 경기 **D-day 카운트다운**, 순위표 **승점 막대 시각화** 추가.
  - **학습 페이지**: 공통 네비 연결 + 용어풀이(💬) 규약 도입.
  - **탐구 허브** `explore/index.html` 신설(게임·중계·직업·커리어 카드 분기).
  - **콘텐츠 4종**:
    - `explore/games.html` — 축구 게임(시뮬/매니지먼트/온라인/e스포츠/능력치 원리/직접 만들기 미션).
    - `explore/broadcast.html` — 월드컵 중계를 **만드는 사람들 관점**(제작 파이프라인·플랫폼별 수익/고민·직업·거버넌스).
    - `explore/jobs.html` — 축구 산업 **생태계 지도** + 비선수 직업 20종 카드(4분야).
    - 커리어 로드맵 4분야: `careers/{tech,media,business,support}.html` — 직업별 하는일·필요 공부/역량·**중학→고교→대학→사회초년→전문가 시기별 로드맵**·전망·"지금 첫걸음".
  - 시각화: mermaid 플로우 + Chart.js(레이더/막대/도넛), **전부 오프라인 폴백(표/숫자)** 동반.
  - **거버넌스** 등 어려운 용어는 💬 한 줄 풀이 부착(요청 반영).
- **왜:** 경기 관전을 넘어 "축구를 게임·방송·직업으로 확장"하며 아들의 흥미를 진로 탐색으로 잇기 위함. 방대해도 좋다는 요청 반영.
- **방법:** 공통 CSS + jobs.html을 본보기로 만든 뒤, 나머지 6페이지를 병렬 서브에이전트로 동시 생성(같은 규약·클래스 강제).
- **어떻게 확인:** 내부 링크 무결성 검사(10개 HTML, 깨진 링크 0), div 태그 균형, app.js 문법·순위 로직 재검증 통과. (따옴표 불일치 버그 1건 수정함)
- **다음 할 일:**
  1. 브라우저에서 탐구 섹션 클릭 동선 + (인터넷 연결 시) 차트/다이어그램 렌더 확인.
  2. GitHub Pages 배포(서브폴더 경로 동작 확인).
  3. 아들 피드백 받아 관심 직업 페이지 심화.

---

## 2026-06-14  프로젝트 셋업 + 프로토타입 → 구조 분해 이식

- **한 일:**
  - 단일파일 프로토타입(`worldcup_2026_app.html`)을 빌드 없는 4파일 구조로 분해 이식.
    - `js/data.js` (경기 데이터 `WC_DATA`), `js/standings.js` (순위 계산), `js/app.js` (화면/이벤트), `css/style.css` (꾸미기), `index.html` (뼈대).
  - 동작은 원본과 100% 동일 유지 (필터·정렬·결과입력·순위 자동계산·localStorage·JSON 입출력·탭).
  - 모든 코드에 **아들용 한글 주석** 추가.
  - **아들 학습 페이지 `learn/index.html`** 신규 작성: 큰 그림 → 데이터 → 순위계산 → 저장 → 바이브 코딩 → 미션 5종(난이도별) → 다음 레벨.
  - 프로젝트 문서 세트 작성: `CLAUDE.md`(Claude용 규칙), `README.md`(사람용), `WORKFLOW.md`(기술/API 상세), 이 `WORKLOG.md`.
  - 원본 2파일 보존: `_prototype_original.html`, `CLAUDE_CODE_MIGRATION.md`.
- **왜:**
  - 목적이 "정식 앱"이 아니라 **아들의 바이브 코딩 입문 + 직접 확장**이라, 빌드/서버 없는 단순 구조가 학습에 최적.
  - 한 파일 덩어리는 초보가 못 읽음 → 역할별로 쪼개 "관심사 분리"를 몸으로 익히게.
- **어떻게 확인:** `index.html` 더블클릭 → 세 탭 표시, 결과 입력 시 순위 갱신, 새로고침 후 점수 유지.
- **다음 할 일 (사람이 고르기):**
  1. 로컬에서 동작 확인 후 GitHub 저장소 만들고 Pages 배포 (→ `README.md` 배포 절차).
  2. 아들과 `learn/` 미션 1(색 바꾸기)부터 같이 해보기.
  3. (선택) 미션 5 = 실시간 결과 자동수집 (→ `WORKFLOW.md` API 가이드). 서버 필요해서 난이도 ↑.

---

## (이전) 프로토타입 단계 — Claude Chat에서

- 단일 HTML 파일로 72+16 경기 일정/결과/순위 앱 작성. localStorage 저장, JSON 입출력.
- React 정식화 계획서(`CLAUDE_CODE_MIGRATION.md`) 작성.
- → 이후 "아들 학습용"으로 방향을 잡고 빌드 없는 구조로 재출발 (위 항목).
